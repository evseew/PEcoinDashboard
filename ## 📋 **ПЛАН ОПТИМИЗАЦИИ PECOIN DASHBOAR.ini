## 📋 **ПЛАН ОПТИМИЗАЦИИ PECOIN DASHBOARD**
### *Комплексные правки архитектуры данных и кэширования*

---

## 🎯 **ОБЩАЯ ЦЕЛЬ**

Упростить и оптимизировать систему получения данных, устранить дублирование запросов, упростить многоуровневое кэширование до логичной 2-уровневой архитектуры.

---

## 🔴 **КРИТИЧНЫЕ ПРОБЛЕМЫ К ИСПРАВЛЕНИЮ**

### **1. Дублирование экосистемы (ПРИОРИТЕТ #1)**

**Проблема:** `DynamicEcosystemCache` дублирует запросы `PublicDashboard` в development режиме

**Решение:**
```typescript
// lib/dynamic-ecosystem-cache.ts
async autoInitialize(): Promise<void> {
  // ✅ ПОЛНОСТЬЮ ОТКЛЮЧИТЬ во всех средах
  console.log('⚠️ DynamicEcosystemCache полностью отключен для предотвращения дублирования')
  return
}
```

### **2. 4-уровневое кэширование балансов (ПРИОРИТЕТ #2)**

**Проблема:** Избыточные уровни: GlobalBalanceCache → ServerCache → CachedTokenBalance → Alchemy

**Решение:** Упростить до 2 уровней:
```typescript
// hooks/use-dashboard-balances.ts - ОБНОВИТЬ
const globalBalanceCache = {
  balances: {} as Record<string, number>,
  TTL: 2 * 60 * 1000 // ✅ УМЕНЬШИТЬ до 2 минут
}

// ✅ УБРАТЬ промежуточные уровни, обращаться напрямую к API
```

### **3. EntityDetail делает лишние запросы (ПРИОРИТЕТ #3)**

**Проблема:** Загружает балансы индивидуально вместо переиспользования batch данных

**Решение:**
```typescript
// components/entity-detail.tsx - ИСПРАВИТЬ
// ❌ УБРАТЬ индивидуальный запрос:
// const balanceResponse = await fetch("/api/token-balances", {
//   body: JSON.stringify({ wallets: [data.wallet_address] })
// })

// ✅ ИСПОЛЬЗОВАТЬ глобальный кэш:
import { globalBalanceCache } from '@/hooks/use-dashboard-balances'

const actualBalance = globalBalanceCache.balances[data.wallet_address] || 0
```

---

## 🟡 **СРЕДНИЕ ПРОБЛЕМЫ К ИСПРАВЛЕНИЮ**

### **4. Неконсистентные ключи кэша**

**Проблема:** Разные форматы ключей в разных местах

**Решение:** Унифицировать через `ServerCache.createKey`:
```typescript
// ✅ ЗАМЕНИТЬ ВО ВСЕХ МЕСТАХ:
// lib/cached-token-balance.ts
const cacheKey = ServerCache.createKey('token-balance', { wallet: owner, mint })

// app/api/pecoin-history/route.ts  
const cacheKey = ServerCache.createKey('tx-history', { wallet: walletAddress, limit: requestedLimit, before: beforeSignature })

// app/api/nft-collection/batch-counts/route.ts
const cacheKey = ServerCache.createKey('nft-count', { wallet })
```

### **5. NFT endpoint дублирование**

**Проблема:** Два endpoint-а `/batch-counts` и `/nft-collection` с разными кэшами

**Решение:** Объединить логику:
```typescript
// app/api/nft-collection/route.ts - ДОБАВИТЬ параметр
export async function POST(request: NextRequest) {
  const { wallets, onlyCount = false } = await request.json()
  
  if (onlyCount) {
    // Возвращаем только counts (быстро)
    return handleBatchCounts(wallets)
  } else {
    // Возвращаем полные NFT данные
    return handleFullNFTs(wallets)
  }
}
```

### **6. Signed URLs кэш слишком долгий**

**Проблема:** 6 часов может быть избыточно для часто меняющихся логотипов

**Решение:**
```typescript
// lib/signed-url-cache.ts
private readonly TTL = 2 * 60 * 60 * 1000 // ✅ УМЕНЬШИТЬ до 2 часов
```

---

## ✅ **УЛУЧШЕНИЯ АРХИТЕКТУРЫ**

### **7. Мониторинг эффективности кэша**

**Добавить метрики:**
```typescript
// app/api/cache-stats/route.ts - РАСШИРИТЬ
export async function GET() {
  return NextResponse.json({
    server_cache: {
      hit_rate: serverCache.getHitRate(),
      total_items: serverCache.getStats().totalItems,
      most_accessed: serverCache.getMostAccessed(10),
      cleanup_stats: serverCache.getCleanupStats()
    },
    global_cache: {
      balance_cache_age: Date.now() - globalBalanceCache.timestamp,
      cached_wallets: Object.keys(globalBalanceCache.balances).length
    },
    recommendations: [
      "Увеличить TTL для часто используемых ключей",
      "Уменьшить TTL для редко используемых данных"
    ]
  })
}
```

### **8. Автоматическая очистка устаревших записей**

**Периодическая очистка:**
```typescript
// lib/server-cache.ts - ДОБАВИТЬ
setInterval(() => {
  const cleaned = serverCache.cleanup()
  if (cleaned > 0) {
    console.log(`🧹 Автоочистка кэша: удалено ${cleaned} устаревших записей`)
  }
}, 5 * 60 * 1000) // Каждые 5 минут
```

### **9. Optimistic UI для всех данных**

**Показывать кэшированные данные мгновенно:**
```typescript
// components/entity-detail.tsx - ДОБАВИТЬ
useEffect(() => {
  // ✅ Показываем кэшированные данные сразу
  const cachedBalance = globalBalanceCache.balances[data.wallet_address]
  if (cachedBalance !== undefined) {
    setEntity(prev => ({ ...prev, balance: cachedBalance }))
  }
  
  // Затем обновляем в фоне если нужно
}, [data.wallet_address])
```

---

## 🔧 **ПЛАН РЕАЛИЗАЦИИ**

### **Этап 1: Критичные исправления (1-2 дня)**
1. ✅ Полностью отключить `DynamicEcosystemCache`
2. ✅ Исправить `EntityDetail` - убрать индивидуальные запросы балансов  
3. ✅ Унифицировать ключи кэша через `ServerCache.createKey`

### **Этап 2: Упрощение архитектуры (2-3 дня)**
1. ✅ Упростить кэширование балансов до 2 уровней
2. ✅ Объединить NFT endpoints логику
3. ✅ Уменьшить TTL для Signed URLs

### **Этап 3: Мониторинг и улучшения (1-2 дня)**
1. ✅ Расширить `/api/cache-stats` метриками
2. ✅ Добавить автоматическую очистку устаревших записей
3. ✅ Внедрить Optimistic UI везде

### **Этап 4: Тестирование и документация (1 день)**
1. ✅ Создать автотесты для ключевых функций кэширования
2. ✅ Обновить документацию API endpoints
3. ✅ Провести нагрузочное тестирование

---

## 📊 **ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ**

### **Производительность:**
- ⚡ **Уменьшение времени загрузки главной на 30-50%**
- 🎯 **Снижение нагрузки на внешние API на 40-60%**  
- 💾 **Увеличение hit rate кэша до 90%+**

### **Поддерживаемость:**
- 🧹 **Упрощение архитектуры с 9 до 4 уровней кэширования**
- 📝 **Унифицированные ключи кэша для лучшей отладки**
- 🔍 **Детальный мониторинг эффективности**

### **UX улучшения:**
- ⚡ **Мгновенное отображение кэшированных данных**
- 🔄 **Плавные фоновые обновления без блокировки UI**
- 📱 **Лучшая отзывчивость на медленных соединениях**

---

## 🎯 **ФИНАЛЬНАЯ АРХИТЕКТУРА**

```
🌐 User Request
    ↓
📱 Component (with Optimistic UI)
    ↓ (if cache miss/stale)
🔗 API Endpoint
    ↓ (unified cache keys)  
🗄️ ServerCache (1-2 min TTL)
    ↓ (if cache miss)
🌐 External APIs (Alchemy/Solana/Supabase)
```

**Простая, эффективная, масштабируемая архитектура! 🚀**